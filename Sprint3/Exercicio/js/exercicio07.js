var nome = String(prompt('Digite o nome correto: '));
var senha = Number(prompt('Digite a senha correta: '));

var name = 'Joao Pedro'
var password = '993451'

if (nome === name && senha === password) {
    document.write('Acesso permitdo!')
} else {
    document.write('Acesso negado!')
}